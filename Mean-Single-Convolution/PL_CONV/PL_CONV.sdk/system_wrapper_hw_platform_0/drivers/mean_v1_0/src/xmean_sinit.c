// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2018.2
// Copyright (C) 1986-2018 Xilinx, Inc. All Rights Reserved.
// 
// ==============================================================

#ifndef __linux__

#include "xstatus.h"
#include "xparameters.h"
#include "xmean.h"

extern XMean_Config XMean_ConfigTable[];

XMean_Config *XMean_LookupConfig(u16 DeviceId) {
	XMean_Config *ConfigPtr = NULL;

	int Index;

	for (Index = 0; Index < XPAR_XMEAN_NUM_INSTANCES; Index++) {
		if (XMean_ConfigTable[Index].DeviceId == DeviceId) {
			ConfigPtr = &XMean_ConfigTable[Index];
			break;
		}
	}

	return ConfigPtr;
}

int XMean_Initialize(XMean *InstancePtr, u16 DeviceId) {
	XMean_Config *ConfigPtr;

	Xil_AssertNonvoid(InstancePtr != NULL);

	ConfigPtr = XMean_LookupConfig(DeviceId);
	if (ConfigPtr == NULL) {
		InstancePtr->IsReady = 0;
		return (XST_DEVICE_NOT_FOUND);
	}

	return XMean_CfgInitialize(InstancePtr, ConfigPtr);
}

#endif

